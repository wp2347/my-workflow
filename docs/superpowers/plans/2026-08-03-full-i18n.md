# 全量 i18n 改造 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消除中英混合界面，所有面向用户的 UI 文案支持中英文切换。

**Architecture:** 在 `zh.json`/`en.json` 按页面分组新增 i18n key（`history.*`/`credentials.*`/`knowledge.*`/`toolbar.*`/`cronNode.*`/`widget.*`/`config.*`），各文件用 `useTranslation()` + `t()` 替换硬编码中文。CronTriggerNode 动态文案用占位符 i18n。Canvas 默认配置值"定时任务"改中性值。

**Tech Stack:** React · i18n（zh.json/en.json）· Vitest（验证）

**测试策略：** 纯文案替换 + JSON 验证，无新逻辑，主要用 typecheck/lint/浏览器验证。

---

## 文件结构

**修改（10 个 UI 文件 + 2 个 locale 文件）：**
- `src/i18n/locales/zh.json` / `en.json` — 新增 key
- `src/app/(dashboard)/layout.tsx` — 3 处
- `src/app/(dashboard)/history/page.tsx` — 6 处
- `src/app/(dashboard)/history/[id]/page.tsx` — 4 处
- `src/app/(dashboard)/credentials/page.tsx` — ~15 处
- `src/app/(dashboard)/knowledge/page.tsx` — ~10 处
- `src/app/(dashboard)/workflow/[id]/page.tsx` — 1 处
- `src/components/panels/Toolbar.tsx` — ~10 处
- `src/components/nodes/CronTriggerNode.tsx` — ~6 处（含动态占位符）
- `src/components/canvas/Canvas.tsx` — 1 处默认值
- `src/app/widget/[workflowId]/page.tsx` — 3 处
- `src/components/panels/NodeConfigPanel.tsx` — ~7 处

---

## Task 1: i18n keys（zh.json + en.json）

**Files:**
- Modify: `src/i18n/locales/zh.json`
- Modify: `src/i18n/locales/en.json`

**这一任务是后续所有任务的基础，必须先完成。** 分两步：先 zh 后 en。

- [ ] **Step 1: zh.json 新增所有 key**

在 `src/i18n/locales/zh.json` 各分组下新增（按语义插入现有分组，保持 JSON 合法）：

`sidebar` 对象内（`extensions` 后）追加：
```json
    "history": "执行历史",
    "credentials": "凭证管理",
    "knowledge": "知识库",
```

`workflows` 对象内追加：
```json
    "completed": "成功",
    "failed": "失败",
    "prev": "上一页",
    "next": "下一页",
```

新增 `history` 顶层对象：
```json
  "history": {
    "title": "执行历史",
    "description": "查看所有工作流的执行记录",
    "noExecutions": "暂无执行记录",
    "noExecutionsDesc": "运行工作流后这里会显示执行历史"
  },
```

新增 `historyDetail` 顶层对象：
```json
  "historyDetail": {
    "notFound": "未找到执行记录",
    "title": "执行详情",
    "completed": "成功",
    "failed": "失败",
    "output": "输出数据"
  },
```

新增 `credentials` 顶层对象：
```json
  "credentials": {
    "title": "凭证管理",
    "description": "AES-256 加密存储，点击眼睛查看明文，点击密钥复制",
    "add": "添加凭证",
    "noCreds": "暂无凭证",
    "noCredsDesc": "添加 API Key 等敏感凭证",
    "copy": "点击复制",
    "view": "点击眼睛查看",
    "copied": "已复制",
    "addTitle": "添加凭证",
    "addDesc": "AES-256 加密存储到数据库",
    "name": "名称",
    "namePlaceholder": "如 DeepSeek API Key",
    "type": "类型",
    "value": "值",
    "scope": "作用域",
    "global": "全局",
    "workflow": "工作流",
    "node": "节点",
    "cancel": "取消",
    "save": "保存",
    "deleteTitle": "删除凭证",
    "deleteDesc": "确定删除 \"{name}\"？不可撤销。",
    "delete": "删除"
  },
```

新增 `knowledge` 顶层对象：
```json
  "knowledge": {
    "title": "知识库",
    "description": "上传文档，向量化后用于 RAG 检索增强生成",
    "addDoc": "添加文档",
    "noDocs": "暂无文档",
    "noDocsDesc": "上传 PDF/TXT/Markdown 构建知识库",
    "uploadFile": "上传文件",
    "manualInput": "或手动输入",
    "manualInputName": "手动输入",
    "docName": "文档名",
    "docNamePlaceholder": "文档名称",
    "content": "内容",
    "contentPlaceholder": "粘贴文档内容...",
    "cancel": "取消",
    "upload": "上传",
    "chunks": "{count} 个分块 · {type}",
    "chunkSize": "{size}字/块"
  },
```

`workflow` 对象内追加：
```json
    "untitled": "未命名工作流",
```

新增 `toolbar` 顶层对象：
```json
  "toolbar": {
    "run": "执行",
    "webhook": "Webhook",
    "webhookTitle": "Webhook URL",
    "webhookDesc": "POST 请求到此地址将触发工作流执行",
    "copied": "已复制",
    "copy": "复制地址",
    "curlExample": "curl 示例：",
    "status": "状态: {status}",
    "duration": "耗时: {duration}ms",
    "error": "   错误: {error}",
    "output": "   输出: {output}",
    "requestFailed": "请求失败: {error}",
    "runResultTitle": "执行结果"
  },
```

新增 `cronNode` 顶层对象：
```json
  "cronNode": {
    "label": "定时",
    "unconfigured": "未配置",
    "hourly": "每小时",
    "daily": "每天 {time}",
    "weekday": "工作日 {time}",
    "weekly": "每周{day} {time}",
    "days": { "0": "日", "1": "一", "2": "二", "3": "三", "4": "四", "5": "五", "6": "六" }
  },
```

新增 `widget` 顶层对象：
```json
  "widget": {
    "requestFailed": "请求失败，请重试",
    "startChat": "发送消息开始对话",
    "inputPlaceholder": "输入消息..."
  },
```

`config` 对象内追加（Merge + HTTP 遗留）：
```json
    "mergeStrategy": "汇聚策略",
    "mergeConcat": "拼接文本",
    "mergeJsonArray": "JSON 数组",
    "mergeFirst": "取第一个",
    "mergeLast": "取最后一个",
    "mergeHint": "将多个上游节点的输出按策略汇聚。常用于 IF/ELSE 分支后合并结果。",
```

注意：`config.authUsername`/`config.authPassword` 已存在（HTTP 节点用过），复用即可，不重复加。

- [ ] **Step 2: en.json 新增所有 key（对应英文）**

`sidebar` 内：
```json
    "history": "History",
    "credentials": "Credentials",
    "knowledge": "Knowledge",
```
`workflows` 内：
```json
    "completed": "Success",
    "failed": "Failed",
    "prev": "Prev",
    "next": "Next",
```
`history`：
```json
  "history": {
    "title": "Execution History",
    "description": "View execution records of all workflows",
    "noExecutions": "No execution records",
    "noExecutionsDesc": "Execution history will appear here after running workflows"
  },
```
`historyDetail`：
```json
  "historyDetail": {
    "notFound": "Execution record not found",
    "title": "Execution Details",
    "completed": "Success",
    "failed": "Failed",
    "output": "Output Data"
  },
```
`credentials`：
```json
  "credentials": {
    "title": "Credentials",
    "description": "AES-256 encrypted storage. Click the eye to view plaintext, click the key to copy",
    "add": "Add Credential",
    "noCreds": "No credentials",
    "noCredsDesc": "Add sensitive credentials like API keys",
    "copy": "Click to copy",
    "view": "Click the eye to view",
    "copied": "Copied",
    "addTitle": "Add Credential",
    "addDesc": "AES-256 encrypted storage to database",
    "name": "Name",
    "namePlaceholder": "e.g. DeepSeek API Key",
    "type": "Type",
    "value": "Value",
    "scope": "Scope",
    "global": "Global",
    "workflow": "Workflow",
    "node": "Node",
    "cancel": "Cancel",
    "save": "Save",
    "deleteTitle": "Delete Credential",
    "deleteDesc": "Delete \"{name}\"? This cannot be undone.",
    "delete": "Delete"
  },
```
`knowledge`：
```json
  "knowledge": {
    "title": "Knowledge Base",
    "description": "Upload documents, vectorize for RAG-enhanced generation",
    "addDoc": "Add Document",
    "noDocs": "No documents",
    "noDocsDesc": "Upload PDF/TXT/Markdown to build a knowledge base",
    "uploadFile": "Upload File",
    "manualInput": "or enter manually",
    "manualInputName": "Manual input",
    "docName": "Document Name",
    "docNamePlaceholder": "Document name",
    "content": "Content",
    "contentPlaceholder": "Paste document content...",
    "cancel": "Cancel",
    "upload": "Upload",
    "chunks": "{count} chunks · {type}",
    "chunkSize": "{size} chars/chunk"
  },
```
`workflow` 内：
```json
    "untitled": "Untitled Workflow",
```
`toolbar`：
```json
  "toolbar": {
    "run": "Run",
    "webhook": "Webhook",
    "webhookTitle": "Webhook URL",
    "webhookDesc": "POST requests to this address trigger the workflow",
    "copied": "Copied",
    "copy": "Copy URL",
    "curlExample": "curl example:",
    "status": "Status: {status}",
    "duration": "Duration: {duration}ms",
    "error": "   Error: {error}",
    "output": "   Output: {output}",
    "requestFailed": "Request failed: {error}",
    "runResultTitle": "Execution Result"
  },
```
`cronNode`：
```json
  "cronNode": {
    "label": "Cron",
    "unconfigured": "Not configured",
    "hourly": "Hourly",
    "daily": "Daily {time}",
    "weekday": "Weekdays {time}",
    "weekly": "Weekly {day} {time}",
    "days": { "0": "Sun", "1": "Mon", "2": "Tue", "3": "Wed", "4": "Thu", "5": "Fri", "6": "Sat" }
  },
```
`widget`：
```json
  "widget": {
    "requestFailed": "Request failed, please retry",
    "startChat": "Send a message to start chatting",
    "inputPlaceholder": "Type a message..."
  },
```
`config` 内：
```json
    "mergeStrategy": "Merge Strategy",
    "mergeConcat": "Concatenate text",
    "mergeJsonArray": "JSON array",
    "mergeFirst": "Take first",
    "mergeLast": "Take last",
    "mergeHint": "Merge upstream node outputs by strategy. Often used to combine IF/ELSE branches.",
```

- [ ] **Step 3: 验证 JSON 合法**

Run: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/zh.json','utf8')); JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json','utf8')); console.log('ok')"`
Expected: 输出 `ok`

- [ ] **Step 4: Commit**

```bash
git add src/i18n/locales/zh.json src/i18n/locales/en.json
git commit -m "feat(i18n): 全量 i18n key（history/credentials/knowledge/toolbar/cronNode/widget）"
```

---

## Task 2: 侧边栏 + 工作流编辑器页

**Files:**
- Modify: `src/app/(dashboard)/layout.tsx`
- Modify: `src/app/(dashboard)/workflow/[id]/page.tsx`

- [ ] **Step 1: layout.tsx 三处改 i18n**

`src/app/(dashboard)/layout.tsx`：
- 「执行历史」Link（第 81 行 `执行历史`）→ `{t("sidebar.history")}`
- 「凭证管理」Link（第 95 行 `凭证管理`）→ `{t("sidebar.credentials")}`
- 「知识库」Link（第 102 行 `知识库`）→ `{t("sidebar.knowledge")}`

（`useTranslation` 已在 layout.tsx 引入，`const { t } = useTranslation()` 已存在。）

- [ ] **Step 2: workflow/[id]/page.tsx「未命名工作流」改 i18n**

`src/app/(dashboard)/workflow/[id]/page.tsx` 第 51 行：
```tsx
{ id: "", name: "未命名工作流", description: "", config: {}, createdAt: "", updatedAt: "" },
```
改为：
```tsx
{ id: "", name: t("workflow.untitled"), description: "", config: {}, createdAt: "", updatedAt: "" },
```
并在组件顶部（`const [loading, setLoading]` 之前）加：
```tsx
const { t } = useTranslation()
```
import 区加：`import { useTranslation } from "@/i18n"`

注意：`useEffect` 内使用了 `t`，由于 `t` 是稳定的（locale store selector），`useEffect` 依赖数组不变（现有依赖是 `[workflowId, isNew, setWorkflowId, setWorkflow]`）——**不需要**把 `t` 加进依赖数组（它是稳定引用，eslint exhaustive-deps 可能提示，但 `t` 来自 store selector 每次渲染稳定；若 lint 报依赖缺失，可接受为 pre-existing 模式或加 `// eslint-disable-next-line`）。实现时如果 lint 报错再处理。

- [ ] **Step 3: typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: PASS（无新错误类别；`set-state-in-effect` 等 pre-existing 保持基线）

- [ ] **Step 4: Commit**

```bash
git add "src/app/(dashboard)/layout.tsx" "src/app/(dashboard)/workflow/[id]/page.tsx"
git commit -m "feat(i18n): 侧边栏 + 工作流编辑器页文案"
```

---

## Task 3: history 页面（列表 + 详情）

**Files:**
- Modify: `src/app/(dashboard)/history/page.tsx`
- Modify: `src/app/(dashboard)/history/[id]/page.tsx`

- [ ] **Step 1: history/page.tsx 改 i18n**

`src/app/(dashboard)/history/page.tsx`（client component，已有 `useTranslation`？——需确认。若没有则加）：
- import 加 `import { useTranslation } from "@/i18n"`（若未引入），组件顶部加 `const { t } = useTranslation()`
- 第 53 行 `<h1 ...>执行历史</h1>` → `<h1 ...>{t("history.title")}</h1>`
- 第 55 行 `查看所有工作流的执行记录` → `{t("history.description")}`
- 第 68 行 `暂无执行记录` → `{t("history.noExecutions")}`
- 第 69 行 `运行工作流后这里会显示执行历史` → `{t("history.noExecutionsDesc")}`
- 第 109 行 `{ex.status === "completed" ? "成功" : ex.status === "failed" ? "失败" : ex.status}` → `{ex.status === "completed" ? t("history.completed") : ex.status === "failed" ? t("history.failed") : ex.status}`
- 第 130 行 `上一页` → `{t("workflows.prev")}`
- 第 140 行 `下一页` → `{t("workflows.next")}`

- [ ] **Step 2: history/[id]/page.tsx 改 i18n**

`src/app/(dashboard)/history/[id]/page.tsx`：
- import 加 `import { useTranslation } from "@/i18n"`，组件顶部加 `const { t } = useTranslation()`
- 第 29 行 `未找到执行记录` → `{t("historyDetail.notFound")}`
- 第 40 行 `{data.workflowName as string || "执行详情"}` → `{data.workflowName as string || t("historyDetail.title")}`
- 第 46 行 `{...(data.status as string) === "completed" ? "成功" : "失败"}` → `{...(data.status as string) === "completed" ? t("historyDetail.completed") : t("historyDetail.failed")}`
- 第 101 行 `输出数据` → `{t("historyDetail.output")}`

- [ ] **Step 3: typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add "src/app/(dashboard)/history/page.tsx" "src/app/(dashboard)/history/[id]/page.tsx"
git commit -m "feat(i18n): history 页面文案"
```

---

## Task 4: credentials 页面

**Files:**
- Modify: `src/app/(dashboard)/credentials/page.tsx`

- [ ] **Step 1: 改 i18n**

`src/app/(dashboard)/credentials/page.tsx`（client component，无 useTranslation？——确认后加）：
- import 加 `import { useTranslation } from "@/i18n"`，组件顶部 `const { t } = useTranslation()`
- 第 76 行 `凭证管理` → `{t("credentials.title")}`
- 第 77 行 AES-256 描述 → `{t("credentials.description")}`
- 第 79 行 `添加凭证` → `{t("credentials.add")}`
- 第 87 行 `暂无凭证` → `{t("credentials.noCreds")}`
- 第 88 行 `添加 API Key 等敏感凭证` → `{t("credentials.noCredsDesc")}`
- 第 89 行 `添加凭证` → `{t("credentials.add")}`
- 第 108 行 `title={revealed[c.id] ? "点击复制" : "点击眼睛查看"}` → `title={revealed[c.id] ? t("credentials.copy") : t("credentials.view")}`
- 第 114 行 `已复制` → `{t("credentials.copied")}`
- 第 134 行 `添加凭证` / `AES-256 加密存储到数据库` → `{t("credentials.addTitle")}` / `{t("credentials.addDesc")}`
- 第 136 行 `名称` / placeholder `如 DeepSeek API Key` → `{t("credentials.name")}` / placeholder={t("credentials.namePlaceholder")}
- 第 137 行 `类型` → `{t("credentials.type")}`
- 第 146 行 `值` → `{t("credentials.value")}`
- 第 147 行 `作用域` → `{t("credentials.scope")}`
- 第 151 行 `全局`/`工作流`/`节点` → `{t("credentials.global")}` / `{t("credentials.workflow")}` / `{t("credentials.node")}`
- 第 157 行 `取消` → `{t("credentials.cancel")}`
- 第 158 行 `保存` → `{t("credentials.save")}`
- 第 165 行 `删除凭证` / `确定删除 "{toDelete?.name}"？不可撤销。` → `{t("credentials.deleteTitle")}` / `{t("credentials.deleteDesc", { name: toDelete?.name || "" })}`
- 第 167 行 `取消` → `{t("credentials.cancel")}`
- 第 168 行 `删除` → `{t("credentials.delete")}`

- [ ] **Step 2: typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add "src/app/(dashboard)/credentials/page.tsx"
git commit -m "feat(i18n): credentials 页面文案"
```

---

## Task 5: knowledge 页面

**Files:**
- Modify: `src/app/(dashboard)/knowledge/page.tsx`

- [ ] **Step 1: 改 i18n**

`src/app/(dashboard)/knowledge/page.tsx`：
- import 加 `import { useTranslation } from "@/i18n"`，组件顶部 `const { t } = useTranslation()`
- 第 42 行 `name || "手动输入"`（form 提交用，是值非 UI——但为一致也改）→ `name || t("knowledge.manualInputName")`
- 第 56 行 `知识库` → `{t("knowledge.title")}`
- 第 57 行 RAG 描述 → `{t("knowledge.description")}`
- 第 58 行 `添加文档` → `{t("knowledge.addDoc")}`
- 第 64 行 `暂无文档` / `上传 PDF/TXT/Markdown 构建知识库` → `{t("knowledge.noDocs")}` / `{t("knowledge.noDocsDesc")}`
- 第 65 行 `添加文档` → `{t("knowledge.addDoc")}`
- 第 75 行 `{d._count?.chunks || 0} 个分块 · {d.type}` → `{t("knowledge.chunks", { count: d._count?.chunks || 0, type: d.type })}`
- 第 79 行 `{d.chunkSize}字/块` → `{t("knowledge.chunkSize", { size: d.chunkSize })}`
- 第 91 行 `添加文档` → `{t("knowledge.addDoc")}`
- 第 93 行 `上传文件` → `{t("knowledge.uploadFile")}`
- 第 94 行 `或手动输入` → `{t("knowledge.manualInput")}`
- 第 95 行 `文档名` / placeholder `文档名称` → `{t("knowledge.docName")}` / placeholder={t("knowledge.docNamePlaceholder")}
- 第 96 行 `内容` / `粘贴文档内容...` → `{t("knowledge.content")}` / placeholder={t("knowledge.contentPlaceholder")}
- 第 97 行 `取消` → `{t("knowledge.cancel")}`
- 第 98 行 `上传` → `{t("knowledge.upload")}`

- [ ] **Step 2: typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add "src/app/(dashboard)/knowledge/page.tsx"
git commit -m "feat(i18n): knowledge 页面文案"
```

---

## Task 6: Toolbar + Canvas 默认值

**Files:**
- Modify: `src/components/panels/Toolbar.tsx`
- Modify: `src/components/canvas/Canvas.tsx`

- [ ] **Step 1: Toolbar.tsx 改 i18n**

`src/components/panels/Toolbar.tsx`（已有 `useTranslation` + `const { t } = useTranslation()`）：
- 第 68 行 `` `状态: ${data.status}` `` → `` t("toolbar.status", { status: data.status }) ``
- 第 69 行 `` `耗时: ${data.durationMs}ms` `` → `` t("toolbar.duration", { duration: data.durationMs }) ``
- 第 75 行 `` `   错误: ${log.error}` `` → `` t("toolbar.error", { error: log.error }) ``
- 第 78 行 `` `   输出: ${out.substring(0, 200)}...` `` → `` t("toolbar.output", { output: out.substring(0, 200) + "..." }) ``
- 第 79 行 `` `   输出: ${out}` `` → `` t("toolbar.output", { output: out }) ``
- 第 85 行 `` `请求失败: ${err instanceof Error ? err.message : String(err)}` `` → `` t("toolbar.requestFailed", { error: err instanceof Error ? err.message : String(err) }) ``
- 第 141 行 `执行` → `{t("toolbar.run")}`
- 第 144 行 `Webhook` → `{t("toolbar.webhook")}`
- 第 156 行 `Webhook URL` → `{t("toolbar.webhookTitle")}`
- 第 157 行 `POST 请求到此地址将触发工作流执行` → `{t("toolbar.webhookDesc")}`
- 第 164 行 `{webhookCopied ? "已复制" : "复制地址"}` → `{webhookCopied ? t("toolbar.copied") : t("toolbar.copy")}`
- 第 167 行 `curl 示例：` → `{t("toolbar.curlExample")}`
- 第 179 行 `执行结果` → `{t("toolbar.runResultTitle")}`

- [ ] **Step 2: Canvas.tsx 默认值改中性值**

`src/components/canvas/Canvas.tsx` 第 175 行：
```ts
return { name: "定时任务", cronExpr: "0 9 * * *", timezone: "Asia/Shanghai", frequency: "daily" }
```
改为：
```ts
return { name: "Cron Job", cronExpr: "0 9 * * *", timezone: "Asia/Shanghai", frequency: "daily" }
```
（这是新建 cron 节点的默认配置值，用中性英文避免默认显示中文。）

- [ ] **Step 3: typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add src/components/panels/Toolbar.tsx src/components/canvas/Canvas.tsx
git commit -m "feat(i18n): Toolbar 文案 + Cron 节点默认值中性化"
```

---

## Task 7: CronTriggerNode 动态文案（占位符 i18n）

**Files:**
- Modify: `src/components/nodes/CronTriggerNode.tsx`

- [ ] **Step 1: 改 i18n + 占位符**

`src/components/nodes/CronTriggerNode.tsx`：
- import 加 `import { useTranslation } from "@/i18n"`
- `formatCron` 函数需改为接收 `t` 参数，或在组件内调用。改为：

```tsx
function formatCron(expr: string, t: (k: string, params?: Record<string, string | number>) => string): string {
  if (!expr) return t("cronNode.unconfigured")
  if (expr === "0 * * * *") return t("cronNode.hourly")
  const parts = expr.split(" ")
  const hour = parts[1] || "9"
  const minute = parts[0] || "0"
  const time = `${hour.padStart(2, "0")}:${minute.padStart(2, "0")}`
  if (expr.includes("* * 1-5")) return t("cronNode.weekday", { time })
  const m = expr.match(/^\S+\s+\S+\s+\*\s+\*\s+(\d)$/)
  if (m) {
    const dayKey = `cronNode.days.${m[1]}`
    return t("cronNode.weekly", { day: t(dayKey), time })
  }
  return t("cronNode.daily", { time })
}
```

组件内：
```tsx
function CronTriggerNode({ data, selected }: NodeProps) {
  const { t } = useTranslation()
  const config = data.config as Record<string, unknown> | undefined
  return (
    ...
        <span className="text-sm font-semibold text-foreground">{t("cronNode.label")}</span>
      ...
        {formatCron((config?.cronExpr as string) || "", t)}
    ...
  )
}
```

- [ ] **Step 2: typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/components/nodes/CronTriggerNode.tsx
git commit -m "feat(i18n): CronTriggerNode 动态文案占位符 i18n"
```

---

## Task 8: widget 页面 + NodeConfigPanel 遗留

**Files:**
- Modify: `src/app/widget/[workflowId]/page.tsx`
- Modify: `src/components/panels/NodeConfigPanel.tsx`

- [ ] **Step 1: widget/[workflowId]/page.tsx 改 i18n**

先读 `src/app/widget/[workflowId]/page.tsx` 确认是否为 client component。若是：
- import 加 `import { useTranslation } from "@/i18n"`，组件顶部 `const { t } = useTranslation()`
- 第 39 行 `"请求失败，请重试"` → `t("widget.requestFailed")`
- 第 51 行 `发送消息开始对话` → `{t("widget.startChat")}`
- 第 64 行 placeholder `输入消息...` → placeholder={t("widget.inputPlaceholder")}

**若是 server component**（无 "use client"）：需加 `"use client"` 或确认它是否已能访问 hooks。若加 "use client" 会导致该页变客户端渲染——检查页面是否有服务端特有逻辑（如读取 DB）。若确实需要 server，则此文件跳过（标注 DONE_WITH_CONCERNS），因为 widget 是嵌入页非主界面。实现时判断。

- [ ] **Step 2: NodeConfigPanel.tsx 遗留文案改 i18n**

`src/components/panels/NodeConfigPanel.tsx`（已有 `useTranslation` + `const { t } = useTranslation()`）：
- 第 528 行 `汇聚策略` → `{t("config.mergeStrategy")}`
- 第 532-535 行 `拼接文本`/`JSON 数组`/`取第一个`/`取最后一个` → `{t("config.mergeConcat")}` / `{t("config.mergeJsonArray")}` / `{t("config.mergeFirst")}` / `{t("config.mergeLast")}`
- 第 540 行 汇聚说明 → `{t("config.mergeHint")}`
- 第 468 行 placeholder `用户名` → placeholder={t("config.authUsername")}
- 第 470 行 placeholder `密码` → placeholder={t("config.authPassword")}
- 第 225 行 `{/* 扩展包折叠区 */}` 是注释，不改。

注意：第 468-470 行有重复的 HTTP 认证块（第 281-288 行已有 i18n 版），确认只改未 i18n 的那处（以实际代码为准，若有重复块都改）。

- [ ] **Step 3: typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add "src/app/widget/[workflowId]/page.tsx" src/components/panels/NodeConfigPanel.tsx
git commit -m "feat(i18n): widget + NodeConfigPanel 遗留文案"
```

---

## Task 9: 全量验证

**Files:** 无（仅验证）

- [ ] **Step 1: 全部单测**

Run: `npm test`
Expected: PASS（84 个测试）

- [ ] **Step 2: 残留中文扫描**

Run: `rg -n "[\u4e00-\u9fa5]" src --glob "*.tsx" -g '!*.test.*'`
Expected: 仅剩允许保留的：
- `LocaleSwitcher.tsx`（切换按钮文案）
- 注释（`{/* ... */}`）
- 服务端组件中的非 UI 文本

逐个确认剩下的每条都是允许保留的。

- [ ] **Step 3: typecheck + lint**

Run: `npm run typecheck && npm run lint`
Expected: 无新增错误

- [ ] **Step 4: 浏览器验证**

1. 中文模式：侧边栏（首页/工作流/模板库/执行历史/凭证管理/知识库/扩展包）、history 列表/详情、credentials、knowledge、编辑器 Toolbar（执行/Webhook/执行结果）、Cron 节点（每周一 9:00）、widget 页 全中文无混合。
2. 切英文：全英文。
3. 特别检查 Cron 节点英文显示 "Weekly Mon 09:00"、credentials 删除弹窗占位符 `{name}` 正确插值。

- [ ] **Step 5: 最终 Commit（如有修复则按 fix: 提交）**

---

## 自检备注

- Spec 覆盖：layout(T2)、history×2(T3)、credentials(T4)、knowledge(T5)、Toolbar(T6)、Canvas(T6)、CronTriggerNode(T7)、widget(T8)、NodeConfigPanel(T8)、i18n keys(T1)、验证(T9)。全覆盖。
- 占位符扫描：无 TBD/TODO。
- 类型一致：`t(key, params)` 占位符 key 名在 T1 定义、T7/T8 使用一致（`cronNode.weekly` 用 `{day}/{time}`、`toolbar.output` 用 `{output}`、`knowledge.chunks` 用 `{count}/{type}`、`credentials.deleteDesc` 用 `{name}`）。
- 风险点标注：`workflow/[id]/page.tsx` 的 `useEffect` 用 `t`（稳定引用，不加依赖）；widget 页可能 server component（Task 8 判断）。
